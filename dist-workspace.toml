[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
#   shell      — curl | sh   (Linux + macOS)
#   powershell — irm ... | iex (Windows)
#   homebrew   — auto-updated Formula in jedi-knights/homebrew-tap
installers = ["shell", "powershell", "homebrew"]
# The homebrew tap to update on every release (repo path: owner/repo)
tap = "jedi-knights/homebrew-tap"
# Publish the homebrew formula from the release workflow.
publish-jobs = ["homebrew"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
